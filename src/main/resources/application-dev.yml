spring:

  jpa:
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/matager?useSSL=false&serverTimezone=UTC
    username: 'root'
    password: 'Spiderman@2'

logging:
  level:
    #    root: debug
    at.orderking.bossApp: debug
    org.apache.tomcat: debug # added for tomcat as abdullah said
#      type: trace
#      SQL: DEBUG

server:
  port: 8888


